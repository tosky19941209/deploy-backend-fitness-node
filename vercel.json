{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/nodemon",
            "config": {
                "includeFiles": [
                    "dist/**"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server.js"
        }
    ]
}